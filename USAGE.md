# 智能文献检索系统 — 使用文档

## 目录

- [环境要求](#环境要求)
- [快速启动](#快速启动)
- [账号与角色说明](#账号与角色说明)
- [功能使用说明](#功能使用说明)
  - [注册与登录](#注册与登录)
  - [文献搜索](#文献搜索)
  - [添加文献](#添加文献)
  - [PDF 智能解析](#pdf-智能解析)
  - [文献详情与收藏](#文献详情与收藏)
  - [个人资料管理](#个人资料管理)
  - [文献审核（管理员）](#文献审核管理员)
  - [用户管理（管理员）](#用户管理管理员)
- [常见问题](#常见问题)

---

## 环境要求

| 软件 | 版本要求 |
|------|----------|
| JDK | 17 及以上 |
| Maven | 3.6 及以上 |
| MySQL | 8.x |
| Elasticsearch | 8.13.x |
| Node.js | 16 及以上 |
| npm | 8 及以上 |

---

## 快速启动

### 第一步：准备数据库

启动 MySQL，创建数据库：

```sql
CREATE DATABASE literature_db DEFAULT CHARACTER SET utf8mb4;
```

在 `src/main/resources/application.yml` 中确认数据库连接配置（默认用户名 root，密码 123456）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/literature_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
```

MyBatis-Plus 会在首次启动时自动建表（需确认已启用 `ddl-auto`），或手动建表参考 [CODE.md](CODE.md) 中的数据库设计章节。

### 第二步：启动 Elasticsearch

进入 Elasticsearch 安装目录，运行：

```bash
# Windows
bin\elasticsearch.bat

# Linux / macOS
bin/elasticsearch
```

等待日志出现 `started` 后，验证是否正常：

```
浏览器访问：http://localhost:9200
正常应返回集群 JSON 信息
```

> 注意：`application.yml` 中配置的 ES 地址为 `http://localhost:9300`，请确认与实际端口一致。

### 第三步：启动后端

```bash
cd "e:/Java project/literature-search-system"
mvn spring-boot:run
```

后端启动成功后监听 **8080** 端口。启动时会自动：
- 检查并创建 ES 索引 `literature`
- 将数据库中所有 status=1（已审核）的文献同步到 ES

### 第四步：启动前端

```bash
cd "e:/Java project/literature-search-system/literature-search-frontend"
npm install
npm run serve
```

前端启动后监听 **8081** 端口。

浏览器访问：**http://localhost:8081**

---

## 账号与角色说明

系统共有三种角色，权限逐级递增：

| 角色值 | 角色名称 | 权限说明 |
|--------|----------|----------|
| 0 | 普通用户 | 搜索文献、收藏文献、查看检索历史 |
| 1 | 文献经略专员 | 普通用户全部权限 + 上传文献、提交修正建议 |
| 2 | 管理员 | 全部权限 + 审核文献、管理用户 |

> 新注册用户默认角色为**普通用户（0）**，角色升级需由管理员在用户管理页操作。

### 默认管理员

首次部署后需手动在数据库将某个用户的 `role` 设为 `2`：

```sql
UPDATE user SET role = 2 WHERE username = '你的用户名';
```

---

## 功能使用说明

### 注册与登录

**注册：**

1. 访问 `http://localhost:8081/register`
2. 填写用户名（唯一）、密码（3-20位）、昵称
3. 点击注册，成功后跳转登录页

**登录：**

1. 访问 `http://localhost:8081/login`
2. 输入用户名和密码
3. 登录成功后跳转首页，token 保存在浏览器 `localStorage` 中，有效期 **7 天**

---

### 文献搜索

首页上方为搜索区域：

1. **关键词搜索**：在搜索框输入关键词，点击「搜索」按钮
   - 支持中文全文搜索，匹配标题、作者、摘要、关键词字段
   - 搜索结果中命中词汇以红色高亮显示

2. **精细筛选**：关键词搜索后可组合以下筛选条件
   - **作者**：精确匹配作者名
   - **期刊**：精确匹配期刊名称
   - **分类筛选**：下拉选择文献分类（政治学/经济学/历史学等）
   - **起始年份 / 结束年份**：限定发表年份范围
   - 点击「应用筛选」生效，「重置筛选」清空所有条件

3. **搜索历史**：每次搜索关键词自动保存（最多保留 20 条），可在个人中心查看，点击可快速重新搜索

---

### 添加文献

首页下方「添加新文献」表单，仅**已登录用户**可使用：

| 字段 | 说明 |
|------|------|
| 标题 | 必填 |
| 作者 | 必填 |
| 期刊 | 选填 |
| 发表年份 | 选填，数字 |
| 分类 | 下拉选择，选填 |
| 关键词 | 选填，多个关键词用**分号**分隔 |
| 摘要 | 选填 |
| 上传文件 | 选填，支持 PDF / DOC / DOCX / TXT，最大 20MB |

填写完成后点击「添加文献」提交。

> 提示：可先上传 PDF 文件使用智能解析功能自动填充字段，再手动补全。

---

### PDF 智能解析

在「添加新文献」表单中点击「选择文件」上传 PDF 后，系统会自动解析并填充以下字段：

| 字段 | 解析来源 |
|------|----------|
| 标题 | PDF 正文中"题目："标记或首段长中文句 |
| 作者 | "作者："后的中文姓名 |
| 发表年份 | 正文中首个 2000-2029 年份数字 |
| 摘要 | "摘要："后的中文内容 |
| 关键词 | "关键词："后的中文内容 |
| 期刊 | 含"学报/杂志/期刊"等后缀的中文词 |
| 分类 | 基于标题+关键词+摘要自动判断 |

**解析说明：**
- 仅解析 PDF 前 5 页内容
- 主要针对中文学术论文优化，英文论文解析效果有限
- 解析结果仅供参考，可手动修改后再提交

---

### 文献详情与收藏

**查看详情：**

在搜索结果列表中点击文献标题，弹出详情窗口，显示：
- 完整标题、作者、期刊、年份、分类、DOI
- 完整摘要
- 附件下载链接（如有）

**收藏/取消收藏：**

- 在搜索结果卡片右上角点击收藏按钮（★）
- 已收藏文献在个人中心「我的收藏」标签页中查看
- 可在收藏列表中点击文献标题直接查看详情，或点击「取消收藏」移除

---

### 个人资料管理

访问地址：`http://localhost:8081/profile`

#### 基本信息

- 修改昵称、邮箱
- 更换头像（支持 JPG / PNG / GIF / WEBP）
  1. 点击「更换头像」选择图片
  2. 图片即时预览
  3. 点击「保存修改」上传生效

#### 修改密码

1. 输入当前密码
2. 输入新密码（3-20位）
3. 再次确认新密码
4. 点击「修改密码」，成功后自动退出，需重新登录

#### 我的贡献（role=1 专员可见）

显示自己上传的全部文献及审核状态：

| 状态 | 说明 |
|------|------|
| 待审核（黄色） | 等待管理员审核 |
| 已通过（绿色） | 审核通过，已可被搜索 |
| 已驳回（红色） | 审核未通过，可查看驳回原因 |

被驳回的文献可点击「重新提交」跳转首页，修改后重新提交。

#### 我的收藏

显示收藏的所有文献，可点击标题查看详情，或点击「取消收藏」移除。

#### 检索历史

显示最近 20 条搜索记录，点击任意记录可直接跳转首页重新搜索该关键词。

---

### 文献审核（管理员）

访问地址：`http://localhost:8081/review`（需 role=2）

**待审核文献列表：**

1. 查看文献标题、作者、期刊、提交时间等基本信息
2. 点击「通过」：文献 status 改为 1，同步至 ES，用户可搜索到
3. 点击「驳回」：弹出输入框填写驳回原因，文献 status 改为 2，专员可在个人中心看到原因

---

### 用户管理（管理员）

访问地址：`http://localhost:8081/admin`（需 role=2）

显示所有用户列表，可进行：

| 操作 | 说明 |
|------|------|
| 修改角色 | 下拉选择将用户角色改为普通/专员/管理员 |
| 禁用 | 禁止用户登录（status=1） |
| 启用 | 恢复被禁用的用户 |
| 重置密码 | 将用户密码重置为 `123456` |
| 永久删除 | 从数据库彻底删除该用户（不可恢复） |

---

## 常见问题

**Q：启动时报错 `Connection refused` 连接 Elasticsearch 失败**

A：Elasticsearch 服务未启动。先运行 `bin\elasticsearch.bat`，等待完全启动后再启动后端。验证方法：浏览器访问 `http://localhost:9200` 能返回 JSON 说明 ES 正常。

---

**Q：搜索不到刚添加的文献**

A：新添加的文献默认 status=0（待审核），需管理员在审核页通过后才会同步至 ES，才可被搜索到。如果是管理员直接添加的文献，检查是否触发了 ES 同步。

---

**Q：PDF 解析结果不准确**

A：解析器基于正则规则针对中文学术期刊优化，对以下情况效果较差：
- 英文论文
- 扫描版 PDF（图片型，无文字层）
- 格式不规范的 PDF

建议解析后人工核对并修改字段内容。

---

**Q：头像上传成功但不显示**

A：确认前端开发服务器的 `/uploads` 代理配置是否正确（`vue.config.js`）：

```js
proxy: {
  '/api': { target: 'http://localhost:8080', changeOrigin: true },
  '/uploads': { target: 'http://localhost:8080', changeOrigin: true }
}
```

修改后需重启前端服务（`npm run serve`）才能生效。

---

**Q：忘记密码怎么办**

A：目前系统无找回密码功能。可联系管理员在用户管理页点击「重置密码」，将密码重置为 `123456`，登录后再修改。

---

**Q：前端页面刷新后跳回登录页**

A：Token 存储在 `localStorage`，清除浏览器数据或 Token 过期（7天）后需重新登录，属于正常行为。

---

**Q：上传文件大小限制是多少**

A：后端配置最大 20MB（`spring.servlet.multipart.max-file-size=20MB`）。
